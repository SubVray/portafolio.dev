---
const { class: className, id } = Astro.props
---

<section
	id={id}
	data-section={id}
	class:list={`section relative ${className} container z-50 mx-auto w-full max-w-full scroll-m-20 md:max-w-2xl lg:max-w-5xl`}
>
	<slot />
</section>
